# Configuration

The configuration file is pidrive.cfg located in /opt/PiDrive/config
It is initialised to default settings if it doesn't already exist and _should_ document every option you can change.

...Currently it's also total bogus. Don't mess with it!
